# Optional voice regeneration; never required for numerical evidence checks.
RAINBOW_TTS_ROOT=/absolute/path/to/your/local/tts
# Configure generation credentials locally only when adding an authorized generation job.
DASHSCOPE_API_KEY=
