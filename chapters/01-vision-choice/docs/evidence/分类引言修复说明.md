# V9 分类引言修复

本次修改 65.00–83.73 秒的教学可视化；重渲染到 90.00 秒以覆盖下一节的交接。

- 原因：beat 3 错误复用了三个实验总览，而音轨已进入图像分类。
- 新画面：新图片 → 已训练网络真实特征响应 → 十类概率；猫狗测试样本连续滑动切换，概率柱插值伸缩；末尾回到同一张猫图，网格收束至 (16,16) 像素，接下一节 RGB 演示。
- 配合原录音的句子时间点驱动画面；不重复平衡杆和棋盘。
- 缩短入口背景匹配溶解至 1 秒，并将其放在图形、标题和字幕下方。这里是末帧匹配溶解，没有新增生成视频。
- 使用 samples[0]（猫，测试索引 0）与 samples[2]（狗，测试索引 12）的保存结果；数字停留值来自真实前向计算，移动过程为展示插值。
- 原 AAC 音轨逐包复用；总长 60,544 帧 / 30 fps，约 33:38.13。

## 交付

- 第一章_看见与选择_V9_分类引言修正版_1080p.mp4：完整修正版。
- V9_猫狗分类引言_局部预览.mp4：全片 1:02–1:30 的 28 秒预览。

## 复现

```powershell
npm run check
node scripts/render_v9.mjs classification-stills
node scripts/render_v9.mjs classification-patch
python scripts/assemble_v9_classification.py
```

源文件：src/scenes/ClassificationIntroV9.tsx；src/FilmV9.tsx。

检查：已检查八个关键阶段的渲染帧；类型检查通过；接缝前后原画帧哈希一致；音频包哈希完全一致。完整文件的解码结果见 classification-checks.json。未渲染 4K。
