# 视频工程清理结果

2026-09-23，共删除 443 个文件/目录目标，覆盖 20,929 个文件，按删除前文件大小合计释放 **19.401 GiB**。

## 保留

- 总览：AI进化史_V11_预览1080p.mp4，及 V11 片头、论文汇总预览。
- 第一章：第一章_看见与选择_V9_分类引言修正版_1080p.mp4，及猫狗分类引言局部预览。
- 当前 master 音轨、160 条正文/片头语音源、现用 public/media、字体、论文、实验数据和 checkpoint。
- 小体积源码/历史文档与真实实验，不因版本号较旧而删除被引用组件。

## 清除

旧版完整成片与重复预览、旧无声拼接片/patch、render-temp、retired-render-cache、bundle、webpack .cache、可重新生成的 premaster、第一章 root/v5/v7 旧 master 和旧 narration。

## 检查

13 项关键文件删除前后 SHA-256 一致；未计划删除的 public/src/checkpoint/实验资产大小及存在性一致；两部最新成片完整解码通过。删除路径逐个验证在两个指定工程内，拒绝任何重解析点。

本地原工程 maintenance/2026-09-23 保存完整删除明细和保护清单。删除是真实释放空间，并非把大文件移到另一个目录。

## 后续重渲染

各项目 `npm run render` 已指向当前全片渲染；第一章新增 render_current.mjs，直接读取现有源素材与 V8 主音轨，不再依赖已退役旧版 MP4 拼接。旧版本若想再输出，需恢复旧音轨或自行再生；当前版本不需要。

GitHub 工作副本的 assets 清单已移除 4 项退役音频，保留 retired-assets-2026-09-23.json 记录；这次没有改变仓库可见性。
