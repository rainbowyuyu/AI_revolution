# 本次整理记录

日期：2026-09-23。

## 范围

- 原项目 `ai_evolution_trailer` → `overview/`，当前 V11。
- 原项目 `ai_evolution_ch01_vision_choice` → `chapters/01-vision-choice/`，当前 V9 分类引言修正版。
- 原项目保持原位置；本次复制和整理，不改写其时间线、视频、实验结果或虚拟环境。
- 第二至六章建立计划、状态和维护入口，未冒充已经完成。

## 保存内容

Remotion/Manim 源码、依赖锁、当前及仍被依赖的旧场景、实验脚本、原始小型结果、渲染用证据、15 个小型 checkpoint、上游固定版本与许可证、字幕/口播/镜头索引、来源台账及共享制作 Skill。

生成记录保留经过清理的 `research/generation-ledger.json`。其中 `reservedCny` 是任务费用预留，不等于供应商已结算账单。临时签名链接、传输信息和凭据不上传。

外部素材登记 1,204 项，按类别为：11 项主音频、4 项字体、162 项媒体、21 项论文页面、984 项单句声音缓存、22 项原 PDF。清单覆盖约 4.64 GiB，默认恢复跳过单句声音缓存；不是把这些文件塞入 Git。

## 为维护新增

- 根系列登记表、六章说明、复现/素材/声音/维护指南。
- 隔离实验启动器及校验数据下载工具。
- SHA-256 素材恢复与新素材登记工具。
- 当前字幕/口播导出工具。
- 第一章完整带声音的当前版本渲染入口；修正两个项目 npm 默认渲染命令。
- GitHub CI：轻量证据检查与两工程 TypeScript 类型检查。
- 不打印凭据的 GitHub 上传工具，支持现有 CLI 或系统 Git 凭据登录。

## 此次实际执行的检查

| 检查 | 结果 |
|---|---|
| 两个工程分别 npm ci + npm run check | 通过 |
| 所选 JSON / Python 语法、仓库路径与登记 | 通过 |
| CNN 混淆矩阵/概率/三种子汇总 | 与保存结果一致 |
| DQN 300 个已保存评估回报的统计 | 与保存结果一致 |
| 180 局四子棋逐步合法性及胜负 | 通过 |
| src 实验聚合与三个主 JSON | 一致 |
| 素材恢复、摘要检查、路径越界拒绝 | 临时目录实测通过 |
| 当前入口文档的相对链接 | 存在 |
| 所选文本的凭据模式、签名 URL、本机路径 | 扫描未发现待处理匹配 |

本次没有重新训练、重新跑模型推理、恢复全部大文件或重渲染成片。历史音视频验收报告保留原版本范围，不冒充此次执行的检查。敏感信息扫描覆盖选中源码和文本的规则匹配，不宣称证明任何内容绝对无隐私风险。
